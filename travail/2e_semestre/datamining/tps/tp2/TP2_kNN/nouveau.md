Nouveau:

```python
tab= ["un", "deux", "trois"]
for i, val in enumerate(tab):
	print(i, val)
```

```python
x = np.arange(-2, 3)
#array([-2, -1,  0,  1,  2])
np.flatnonzero(x)
#array([0, 1, 3, 4])
```
